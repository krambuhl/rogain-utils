var test = require('tape');
var templates = require('./fixtures/templates.json');
var createFrame = require('../dist').createFrame;

test.skip('createFrame(tree)', function(t) {
  t.plan(0);
});

test.skip('createFrame(tree, locals)', function(t) {
  t.plan(0);
});